import { test } from './fixtures/base';

test.describe('with-ssr-react example', () => {
  test.skip(
    true,
    'SSR example requires separate server start: cd examples/with-ssr-react && node server.js',
  );
});
