import { test } from './fixtures/base';

test.describe('with-module-federation-react example', () => {
  test.skip(
    true,
    'Module Federation requires 3 separate servers - run manually: cd examples/with-module-federation-react && pnpm start',
  );
});
